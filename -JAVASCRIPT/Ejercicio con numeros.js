/*Crea un código JavaScript para evitar el problema del redondeo que hemos tenido en el código anterior.
Para ello define los decimales como enteros y realiza la operación para mostrar el decimal sólo en el momento de mostrar el 
resultado por pantalla.*/
var malRedondeo= 0.3 - 0.2;
alert("JavaScript redondeo de forma diferente y obtuvo " + malRedondeo+".\nSi somos mas especificos...")

var resultado = (3 - 2) / 10;
alert("Se espera obtener 0.1, y en efecto es "+resultado)